listener:
  address: ":5432"
  max_connections: 1000
  read_timeout: 30s
  write_timeout: 30s

backend:
  primary:
    address: "localhost:5433"
  replicas:
    - address: "localhost:5434"

pool:
  primary_size: 10
  replica_size: 20